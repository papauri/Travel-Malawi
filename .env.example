# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Passwords for the seeded test accounts (scripts/seed-accounts.mjs).
# Copy this file to .env and set real values, or leave them unset and the
# script will generate one per account and print it once.
HOST_PASSWORD="MY_HOST_PASSWORD"
ADMIN_PASSWORD="MY_ADMIN_PASSWORD"
MANAGER_PASSWORD="MY_MANAGER_PASSWORD"
TRAVELLER_PASSWORD="MY_TRAVELLER_PASSWORD"

# Path to a Firebase Admin SDK service-account JSON, for scripts/*.mjs.
# Console -> Project settings -> Service accounts -> Generate new private key.
GOOGLE_APPLICATION_CREDENTIALS="C:/path/to/service-account.json"
